/** @type {import('next').NextConfig} */
const nextConfig = {
  // Pose detection + analysis run entirely client-side (MediaPipe WASM from CDN),
  // so no server config is needed.
};

export default nextConfig;
