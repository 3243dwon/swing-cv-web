# AI coaching (opt-in feature) — server-side only. Copy to .env.local and fill in.
# The key is used exclusively by app/api/coach/route.ts; it is never exposed to the browser.
# Requires an org with 30-day data retention (Claude Fable 5 is not available under zero retention).
ANTHROPIC_API_KEY=
